services:
  discord-bot:
    build: ./discord-bot
    restart: unless-stopped
    networks:
      - griffith-connect
    depends_on:
      - redis
  redis:
    image: redis:latest
    restart: unless-stopped
    networks:
      - griffith-connect
  
networks:
  griffith-connect: